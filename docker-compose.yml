version: '3.8'

services:

  site:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:80"
    env_file:
      - ./.env.dev
    depends_on:
      - db

  db:
    image: postgres:alpine
    volumes:
      - bingo_memes_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.dev

volumes:
  bingo_memes_data:
